dry_run: true
operations:
  - op: upsert
    kind: desired_node
    key: {slug: agdoomed1}
    values: {lifecycle: retired}
  - op: upsert
    kind: desired_compute_instance
    key: {desired_node: agdoomed1}
    values: {desired_presence: absent}
