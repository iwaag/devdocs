observerを使って実際のエージェントプロセスを実行してみる。

study musicgen公開されたばかりのYuE2を使い、基本的な使い方を確認の上、いくつか安定した品質の音楽を出力する条件を探らせる。
モデルのダウンロードや実際の生成のときにobserverを使う機会があるはずだ。

今回はobserverのテストが目的なので、明示的にobserverを使うよう指示してもいい。ただobserverの使い方自体は依頼時のプロンプトではなく、事前に与えられるべきだ。observerはあらゆるエージェントが使用する可能性があるので、それぞれのguideに書くのではなく、agagのcliなどから使いかを一律のコマンドで参照できるようにすべきかもしれない。