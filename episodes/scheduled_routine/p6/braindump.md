Studyパターンのプロジェクトに対する新たなルーチンを加えたい。
arxivの調査ですでに作成したsummaryとmanualを使い、ローカル実行可能なものに関しては、新規giteaリポジトリをもとにプロジェクトフォルダ内に"localtest-<paper id>"を作って、このワークスペースをもとにローカル検証を試みるという作業を加えたい。
これは多くの場合、ローカルPCへのモデルダウンロードやツールインストール、サービス起動などが必要になるはず。cagentに依頼しても基本cagentgはread系のみで、write系の操作を人間やOmni Agentのnctl操作無しで行う方法はないので、エージェント目線では上位開発主体による実行待ちというフェーズが発生するはず。dockerなどで自前でcontainer起動するするのはむしろ推奨したいぐらいだが、その場合後でcluster intentにdesired stateとして事後的に反映できるよう、報告させる、または検知する必要がある。