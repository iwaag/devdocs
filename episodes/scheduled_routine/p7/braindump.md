arxiv trendの studyプロジェクトで５件の調査を一つのセッションで実行させてみてclaude codeのハーネス独自の並列実行が可能か確認したい。明示的に言わずともやってくれそうだが、今回は明示的に指示して並列実行する。
workplan的には論文を５つ選定するのと実際の調査は別のworkとして分離するのがいいと思う。

local testルーチンはローカルのリソースが競合するはずなのでスコープ外。