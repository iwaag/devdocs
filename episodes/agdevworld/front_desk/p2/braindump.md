agfrontのcharacter_talkの仕組みを大きく変えたい。

1. agdevworld内の.localのどこかにキャラクター設定や背景グラフィックスのフォルダをcloneし、参照できる仕組みを作る。これは動作時に参照できるのが望ましいが、phaserのシーン構築時にビルドが必要ならそれでもいい。URLは以下、これはあとで差し替え可能なようにファイルなどに設定可能にする。
https://github.com/iwaag/agdevworld-settings.git
参考までに、プロジェクトルートにagdevworld-settingsに展開中。

2. 上のフォルダをagentからも参照できるようにし、それぞれのエージェントのキャラクター設定をもとに、frontの発話だけではなく、その返答に関するautolabなどの連携先での会話履歴を参照し、短いエージェントキャラクター同士の会話を何らかのフォーマットで生成できるようにする。その会話フォーマットをもとに、agdevworldのfront deskシーンの中でグラフィックノベルの会話風に再現。frontの発言は今まで通り表示、front以外の発話は左上に顔グラフィックとテキストボックスという形で表示。

3. 会話ログは顔グラフィック付きで表示するよう変更。